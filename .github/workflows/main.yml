name: Seu Workflow

on:
  push:
    branches:
      - main

jobs:
  seu-job:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repositório
      uses: actions/checkout@v2

    - name: Adicionar novo step
      shell: bash
      env:
        SUPER_SECRET: ${{ secrets.SECRET }}
      run: |
        echo "$SUPER_SECRET"
        echo $SUPER_SECRET
